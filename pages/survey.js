// Copy the ENTIRE property-survey-complete artifact here
export default function Survey() {
  return (
    <div dangerouslySetInnerHTML={{ __html: `
      <!-- PASTE COMPLETE PROPERTY SURVEY HTML HERE -->
    `}} />
  );
}
